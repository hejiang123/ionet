<%
var methodCommentText = tsDoc(methodComment);
var returnCommentText = tsDoc(returnComment);
%>

    /**
<% if (methodCommentText != "") { %>     * ${methodCommentText}
<% } %>     * @param callback<% if (returnCommentText != "") { %> ${returnCommentText}<% } %> (returnType: ${returnDataIsList?"list of "}{@link ${returnDataName}})
     * @returns {@link RequestCommand} request command
     * @example
     * ${actionSimpleName}.of${actionMethodName}(result => {
     *     // your biz code.
     *     result.log("hello ioGame!");
<% if (returnCommentText != "") { %>     *     // ${returnCommentText}
<% } %>
     *     ${exampleCode}
     *     result.log(value);
     * });
     */
    static of${actionMethodName}(callback: (result: ResponseResult) => void): RequestCommand {
        return RequestCommand.of${sdkMethodName!}(this.${memberCmdName}).onCallback(callback).execute();
    }

    /**
<% if (methodCommentText != "") { %>     * ${methodCommentText}
<% } %>     * @returns {@link ResponseResult}<% if (returnCommentText != "") { %> ${returnCommentText}<% } %> (returnType: ${returnDataIsList?"list of "}{@link ${returnDataName}})
     * @example
     * const result = await ${actionSimpleName}.ofAwait${actionMethodName}();
     * // your biz code.
     * result.log("hello ioGame!");
<% if (returnCommentText != "") { %>     * // ${returnCommentText}
<% } %>
     * ${exampleCode}
     * result.log(value);
     */
    static async ofAwait${actionMethodName}(): Promise<ResponseResult> {
        return await RequestCommand.ofAwait${sdkMethodName!}(this.${memberCmdName});
    }